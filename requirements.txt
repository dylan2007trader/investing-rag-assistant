# Core (always required) — the app runs with just these using TF-IDF embeddings
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.24
scikit-learn>=1.3
pydantic>=2.0

# Grounded generation (optional) — set ANTHROPIC_API_KEY to enable
anthropic>=0.34

# Semantic embeddings (recommended) — enables true vector search.
# Larger install; the app auto-falls back to TF-IDF if this isn't present.
# sentence-transformers>=2.6
